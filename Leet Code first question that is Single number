#include<iostream>
using namespace std;
int singleNumber(vector<int>& nums) {
        int arr[5]={1,2,1,2,4};
        int ans=0;
        for(int val:nums){
            ans=ans^val;
        }
        return ans;
}
